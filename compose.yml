services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.6.4
    command: start-dev
    volumes:
      - kc-data:/opt/keycloak
    env_file:
      - .env
    ports:
      - "8080:8080"


volumes:
  kc-data:
